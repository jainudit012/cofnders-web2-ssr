import { BadInputDirective } from './bad-input.directive';

describe('BadNameDirective', () => {
  it('should create an instance', () => {
    const directive = new BadInputDirective();
    expect(directive).toBeTruthy();
  });
});
