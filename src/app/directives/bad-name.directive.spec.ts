import { BadNameDirective } from './bad-name.directive';

describe('BadNameDirective', () => {
  it('should create an instance', () => {
    const directive = new BadNameDirective();
    expect(directive).toBeTruthy();
  });
});
